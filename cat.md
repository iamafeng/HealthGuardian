# 猫咪种类与生成提示词

### 通用技术要求 (适用于所有猫咪)
- **图片格式:** PNG，必须带有**真实的透明背景** (Alpha通道)，绝对不能是白色或棋盘格背景。
- **整体尺寸:** 精确为 **256x256 像素**。
- **风格:** 纯粹的像素艺术 (Pixel Art)，边缘锐利，无抗锯齿、无模糊。
- **布局:** 4x4 的网格布局，共16帧，每一帧的尺寸为 **64x64 像素**。
- **动画内容 (从上到下，从左到右):**
    - **第1行 (帧0-3):** “空闲” (Idle) 动画，如站立、眨眼、尾巴轻摇。
    - **第2行 (帧4-7):** “行走” (Walk) 动画，四肢交替的行走循环。
    - **第3行 (帧8-11):** “玩耍” (Play) 动画，如扑、跳、打滚。
    - **第4行 (帧12-15):** “睡觉” (Sleep) 动画，如蜷缩、打呼噜。

---

### 1、橘猫 (Orange Tabby)
**描述：** 活泼可爱的橘色虎斑猫，是常见的家猫类型。
**提示词 (Prompt):**
```
一只可爱的橘色虎斑猫的像素艺术游戏精灵图。严格遵守以下要求：PNG格式，真实的透明背景。图片尺寸必须是256x256像素。在4x4的网格中包含16个动画帧，每一帧都是64x64像素。动画需包括4帧空闲、4帧行走、4帧玩耍和4帧睡觉。风格为边缘锐利的像素艺术，无模糊。
```

---

### 2、英短蓝猫 (British Shorthair - Blue)
**描述：** 身体圆润矮胖，毛发为纯蓝色（灰色），表情憨态可掬。
**提示词 (Prompt):**
```
一只英短蓝猫的像素艺术游戏精灵图，特征是身体滚圆、短毛、纯灰色。严格遵守以下要求：PNG格式，真实的透明背景。图片尺寸必须是256x256像素。在4x4的网格中包含16个动画帧，每一帧都是64x64像素。动画需包括4帧空闲、4帧行走、4帧玩耍和4帧睡觉。风格为边缘锐利的像素艺术，无模糊。
```

---

### 3、三花猫 (Calico)
**描述：** 身上有白色、黑色、橘色三种颜色补丁的猫。
**提示词 (Prompt):**
```
一只三花猫（白、黑、橘三色）的像素艺术游戏精灵图。严格遵守以下要求：PNG格式，真实的透明背景。图片尺寸必须是256x256像素。在4x4的网格中包含16个动画帧，每一帧都是64x64像素。动画需包括4帧空闲、4帧行走、4帧玩耍和4帧睡觉。风格为边缘锐利的像素艺术，无模糊。
```

---

### 4、暹罗猫 (Siamese)
**描述：** 身体为浅色，但脸部、耳朵、四肢和尾巴是深色（重点色），拥有一双蓝色的眼睛。
**提示词 (Prompt):**
```
一只暹罗猫的像素艺术游戏精灵图，具有重点色特征和蓝色眼睛。严格遵守以下要求：PNG格式，真实的透明背景。图片尺寸必须是256x256像素。在4x4的网格中包含16个动画帧，每一帧都是64x64像素。动画需包括4帧空闲、4帧行走、4帧玩耍和4帧睡觉。风格为边缘锐利的像素艺术，无模糊。
```

---

### 5、纯黑猫 (Black Cat)
**描述：** 通体乌黑，眼睛可以是黄色或绿色，充满神秘感。
**提示词 (Prompt):**
```
一只纯黑色的猫的像素艺术游戏精灵图，眼睛为亮黄色。严格遵守以下要求：PNG格式，真实的透明背景。图片尺寸必须是256x256像素。在4x4的网格中包含16个动画帧，每一帧都是64x64像素。动画需包括4帧空闲、4帧行走、4帧玩耍和4帧睡觉。风格为边缘锐利的像素艺术，无模糊。
```

---

### 6、布偶猫 (Ragdoll)
**描述：** 体型较大，长毛，通常有重点色和蓝色的眼睛，性格温顺。
**提示词 (Prompt):**
```
一只漂亮的布偶猫的像素艺术游戏精灵图，长毛，有重点色，蓝眼睛。严格遵守以下要求：PNG格式，真实的透明背景。图片尺寸必须是256x256像素。在4x4的网格中包含16个动画帧，每一帧都是64x64像素。动画需包括4帧空闲、4帧行走、4帧玩耍和4帧睡觉。风格为边缘锐利的像素艺术，无模糊。
```