# HealthGuardian 快速运行指南 (V4.3 进化版) 🚀

> **⚠️ 开发者备忘录 (Developer Rules)**
> **【双文档同步约束】所有的项目启动、打包等相关脚本命令请务必统一记录在本文档中。每次新功能开发或脚本、库表修改完成后，必须立即同步更新 `快速运行.md` 和 `README.md`！**
> **【数据库脚本准则】任何 SQL 脚本的变更，必须在本文档维护最完整的全量建表 SQL；同时必须单列一块 [增量更新 SQL]，供历史版本用户升级执行，不可遗漏。**
> **【桌面端同步约束】每次 Web 页面功能开发完成后，必须同步检查并更新 Electron 桌面端：若新功能涉及 localStorage 桥接键（如灵动岛 widget），需同步更新 `widget.html`；若涉及托盘菜单、IPC 通道或全局快捷键，需同步更新 `electron/main.js`。桌面端功能集必须与 Web 端保持一致。**

本文档提供"个人效能监控系统"的完整全栈环境搭建指南。

---

## 0. 项目目录结构 📂

```text
HealthGuardian/
├── pom.xml                         # Maven 构建配置
├── 快速运行.md                      # 本文档（运维与开发指南）
├── README.md                       # 项目介绍文档
├── todo.md                         # 功能待办清单
├── deploy/                         # 【重要】新版 JAR 上传到此处，由 start.sh 自动部署
├── bak/                            # 历史版本备份目录（脚本自动生成，保留最近 3 个）
├── electron/                       # Electron 桌面端封装
│   ├── main.js                     # Electron 主进程（窗口/托盘/后端探活）
│   ├── config.json                 # 默认后端地址配置
│   ├── error.html                  # 后端未就绪时的错误/配置页
│   └── package.json                # Electron NPM 依赖
├── sh/                             # Linux 服务器运维脚本库
│   ├── start.sh                    # 智能启动/更新脚本（优先检测 deploy/）
│   ├── stop.sh                     # 安全停止服务
│   ├── status.sh                   # 服务状态监控
│   └── rollback.sh                 # 一键回滚至上个备份版本
└── src/main/
    ├── java/com/healthguardian/
    │   ├── HealthGuardianApplication.java  # Spring Boot 启动入口
    │   └── ReminderController.java         # 全部 REST API（含 @PostConstruct 自动建表）
    └── resources/
        ├── application.properties          # 数据库/端口配置
        └── static/                         # 前端静态资源（由 Spring Boot 直接托管）
            ├── index.html                  # 主界面
            ├── manifest.json               # PWA 配置
            ├── sw.js                       # Service Worker
            ├── css/style.css               # 全局样式（含 4 主题覆盖规则）
            ├── js/
            │   ├── app.js                  # 核心逻辑（App/UI/Pet/AmbientSound/Workout 等模块）
            │   ├── cv.js                   # TF.js 姿态检测引擎
            │   ├── tf-core.min.js          # TensorFlow.js 核心（本地）
            │   ├── tf-converter.min.js     # TF.js 模型转换器（本地）
            │   ├── tf-backend-webgl.min.js # TF.js WebGL 后端（本地）
            │   ├── pose-detection.min.js   # MoveNet 姿态检测（本地）
            │   └── chart.js                # Chart.js 图表库（本地）
            └── workout/                    # 调息动作 SVG 图示
                ├── neck.svg / chest.svg / squat.svg
                ├── shoulder.svg / wrist.svg / eyes.svg / back.svg
```

---

## 1. 数据库初始化 (MySQL) 💎

### 1.1 增量更新脚本 (老用户升级必跑)
如果您的数据库已经是 V1.0 / V2.0 老版本，执行以下脚本即可平滑升级，无需删除数据：
```sql
USE health_guardian;

-- ── V1.0 → V2.0：给用户表增加 webhook 及其开关 ──────────────────────────────
ALTER TABLE t_user ADD COLUMN IF NOT EXISTS webhook_url VARCHAR(500) DEFAULT NULL COMMENT '推送链路' AFTER secret_key;
ALTER TABLE t_user ADD COLUMN IF NOT EXISTS is_webhook_enabled TINYINT(1) DEFAULT 1 COMMENT '推送开关' AFTER webhook_url;
-- 给提醒配置表增加单项开关控制
ALTER TABLE t_reminder_config ADD COLUMN IF NOT EXISTS is_enabled TINYINT(1) DEFAULT 1 COMMENT '协议是否启用' AFTER interval_minutes;

-- ── V2.x → V3.5：新增健康搭子关系表 ─────────────────────────────────────────
-- 注意：Spring Boot 启动时已通过 @PostConstruct 自动执行 CREATE TABLE IF NOT EXISTS，
-- 此脚本供手动检查/离线初始化使用，可重复执行（幂等）。
CREATE TABLE IF NOT EXISTS t_partner (
    id          BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_key    VARCHAR(255) NOT NULL COMMENT '发起人 secret_key',
    partner_key VARCHAR(255) NOT NULL COMMENT '搭子 secret_key',
    created_at  DATETIME DEFAULT NOW() COMMENT '绑定时间',
    UNIQUE KEY uk_pair (user_key, partner_key)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='健康搭子关系表';

-- ── V3.5 → V4.0：隐藏成就系统 ────────────────────────────────────────────────
-- 注意：@PostConstruct 自动执行，此处供手动核查
ALTER TABLE t_achievement ADD COLUMN IF NOT EXISTS is_hidden TINYINT(1) DEFAULT 0 COMMENT '是否隐藏成就';

INSERT IGNORE INTO t_achievement (code, name, description) VALUES
('MIDNIGHT_GHOST', '午夜幽灵', '🔒 隐藏成就：在深夜 00:00-04:00 完成打卡'),
('HYDRO_CHAMPION', '补水冠军', '🔒 隐藏成就：单日补给达到 10 次'),
('DAWN_WARRIOR',   '黎明战士', '🔒 隐藏成就：07:00 前完成今日首次打卡'),
('PET_LOVER',      '宠物达人', '🔒 隐藏成就：通过小怪兽喂水 5 次');

UPDATE t_achievement SET is_hidden=1 WHERE code IN ('MIDNIGHT_GHOST','HYDRO_CHAMPION','DAWN_WARRIOR','PET_LOVER');

-- ── V4.3：宠物经济系统 ──────────────────────────────────────────────────────
ALTER TABLE t_user ADD COLUMN IF NOT EXISTS coins INT DEFAULT 0 COMMENT '小鱼干币';
ALTER TABLE t_user ADD COLUMN IF NOT EXISTS selected_cat VARCHAR(50) DEFAULT 'cat_OrangeTabby' COMMENT '当前猫咪';
ALTER TABLE t_user ADD COLUMN IF NOT EXISTS unlocked_cats VARCHAR(1000) DEFAULT 'cat_OrangeTabby' COMMENT '已解锁猫咪（逗号分隔）';
```

### 1.2 全量建库建表脚本 (新用户一次性跑完)
请在 MySQL 客户端中执行以下全量脚本。
**注意**：建议先执行 `DROP DATABASE IF EXISTS health_guardian;` 确保结构纯净。

```sql
-- 1. 创建并进入数据库
CREATE DATABASE IF NOT EXISTS health_guardian DEFAULT CHARACTER SET utf8mb4;
USE health_guardian;

-- 2. 用户账号表 (V2.0 增强版)
CREATE TABLE IF NOT EXISTS t_user (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE COMMENT '账号代号',
    password_hash VARCHAR(64) NOT NULL COMMENT '哈希密钥',
    secret_key VARCHAR(100) NOT NULL COMMENT '安全令牌',
    webhook_url VARCHAR(500) DEFAULT NULL COMMENT '推送链路',
    is_webhook_enabled TINYINT(1) DEFAULT 1 COMMENT '推送开关',
    avatar_id VARCHAR(50) DEFAULT 'avatar_1.png' COMMENT '外观ID',
    last_active_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后活跃时间',
    coins INT DEFAULT 0 COMMENT '小鱼干币',
    selected_cat VARCHAR(50) DEFAULT 'cat_OrangeTabby' COMMENT '当前猫咪',
    unlocked_cats TEXT DEFAULT 'cat_OrangeTabby' COMMENT '已解锁猫咪（逗号分隔）',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_key (secret_key),
    INDEX idx_user_active (last_active_at)
) ENGINE=InnoDB;

-- 3. 提醒配置表
CREATE TABLE IF NOT EXISTS t_reminder_config (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) DEFAULT '匿名用户',
    secret_key VARCHAR(100) NOT NULL,
    remind_type VARCHAR(50) NOT NULL COMMENT 'DRINK/SEDENTARY',
    interval_minutes INT NOT NULL,
    is_enabled TINYINT(1) DEFAULT 1,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_config_key (secret_key)
) ENGINE=InnoDB;

-- 4. 执行记录日志
CREATE TABLE IF NOT EXISTS t_reminder_log (
    id INT AUTO_INCREMENT PRIMARY KEY,
    secret_key VARCHAR(100) NOT NULL,
    remind_type VARCHAR(50) NOT NULL,
    completed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_log_key (secret_key)
) ENGINE=InnoDB;

-- 5. 番茄钟专注表
CREATE TABLE IF NOT EXISTS t_pomodoro_log (
    id INT AUTO_INCREMENT PRIMARY KEY,
    secret_key VARCHAR(100) NOT NULL,
    duration_minutes INT DEFAULT 25,
    completed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_pomo_key (secret_key)
) ENGINE=InnoDB;

-- 6. 成就系统基础数据
CREATE TABLE IF NOT EXISTS t_achievement (
    id INT AUTO_INCREMENT PRIMARY KEY,
    code VARCHAR(50) NOT NULL UNIQUE,
    name VARCHAR(50) NOT NULL,
    description VARCHAR(255),
    is_hidden TINYINT(1) DEFAULT 0 COMMENT '是否隐藏成就'
) ENGINE=InnoDB;

CREATE TABLE IF NOT EXISTS t_user_achievement (
    id INT AUTO_INCREMENT PRIMARY KEY,
    secret_key VARCHAR(100) NOT NULL,
    achievement_code VARCHAR(50) NOT NULL,
    achieved_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_ach (secret_key, achievement_code)
) ENGINE=InnoDB;

-- 8. 健康搭子关系表 (V3.5 新增)
-- 注：Spring Boot 启动时由 @PostConstruct 自动建表，此处记录供备档和手动部署使用
CREATE TABLE IF NOT EXISTS t_partner (
    id          BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_key    VARCHAR(255) NOT NULL COMMENT '发起人 secret_key',
    partner_key VARCHAR(255) NOT NULL COMMENT '搭子 secret_key',
    created_at  DATETIME DEFAULT NOW() COMMENT '绑定时间',
    UNIQUE KEY uk_pair (user_key, partner_key)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='健康搭子关系表';

-- 9. 初始化全套荣誉勋章（含 V4.0 隐藏成就）
INSERT IGNORE INTO t_achievement (code, name, description, is_hidden) VALUES 
('EARLY_BIRD',         '早起鸟',   '08:30 前开启今日首个协议',                    0),
('NIGHT_OWL',          '夜猫子',   '22:00 后依然在执行协议',                       0),
('WATER_BUFFALO',      '大水牛',   '单日生物补给达到 8 次',                        0),
('PERSISTENCE',        '不倒翁',   '连续 7 天坚持监控生命体征',                     0),
('FOCUS_MASTER',       '专注达人', '累计完成 5 次深度专注协议',                     0),
('PRODUCTIVITY_BEAST', '效率怪兽', '累计完成 10 次专注协议',                       0),
('STRETCH_EXPERT',     '拉伸达人', '累计完成 20 次调息指引',                       0),
('COMMUNITY_STAR',     '系统公民', '成功建立加密账号身份',                         0),
-- V4.0 隐藏成就
('MIDNIGHT_GHOST',     '午夜幽灵', '🔒 隐藏成就：在深夜 00:00-04:00 完成打卡',     1),
('HYDRO_CHAMPION',     '补水冠军', '🔒 隐藏成就：单日补给达到 10 次',             1),
('DAWN_WARRIOR',       '黎明战士', '🔒 隐藏成就：07:00 前完成今日首次打卡',        1),
('PET_LOVER',          '宠物达人', '🔒 隐藏成就：通过小怪兽喂水 5 次',            1);
```

---

## 2. 核心配置与启动 (Windows)

1.  **修改配置**：系统默认采用环境变量 `${DB_USER:root}` 等形式。如果不需要改动，可以直接使用本地默认的 `root/123456`。若为生产环境，**强烈建议在 jar 包同级目录的 `config/` 文件夹下放置同名 `application.properties` 文件来覆盖默认配置**，如下所示：

    ```properties
    spring.datasource.url=jdbc:mysql://你的生产IP:3306/health_guardian?useSSL=false&allowPublicKeyRetrieval=true
    spring.datasource.username=生产账号
    spring.datasource.password=生产密码
    ```

2.  **本地/开发环境启动方式**：

    *   **方式一：IDEA / 前端开发调试 (推荐)**
        在 IntelliJ IDEA 或 Eclipse 中编辑该项目的运行配置（Run Configuration），在 `Environment variables` 里填入对应真实配置，如：
        `DB_URL=jdbc:mysql://IP:3306/health_guardian?useSSL=false;DB_USER=root;DB_PASS=你的密码`
        然后直接点击 `Run` 启动即可。

    *   **方式二：通过命令行参数临时覆盖 (终端启动)**
        通过 Maven 打包后，可以在执行 `jar` 包时直接通过 `--` 参数传递真实的配置（不需要改动 `application.properties` 代码文件）：
        ```bash
        mvn clean package
        java -jar target/health-guardian-0.0.1-SNAPSHOT.jar --spring.datasource.url="jdbc:mysql://你的真实IP:3306/health_guardian?useSSL=false" --spring.datasource.username="真实账号" --spring.datasource.password="真实密码"
        ```

3.  **访问入口**：`http://localhost:8081`

---

## 3. 桌面级突破 (Electron App) 🌟

> **📥 用户直接下载**：[https://github.com/iamafeng/HealthGuardian/releases/latest/download/HealthGuardian-Setup.exe](https://github.com/iamafeng/HealthGuardian/releases/latest/download/HealthGuardian-Setup.exe)

### 3.1 本地开发运行

```bash
cd electron
npm install      # 首次安装依赖
npm run start    # 开发模式启动（连接本地后端）
```

### 3.2 本地手动打包 .exe

```bash
cd electron
npm run dist
# 产物路径：electron/dist/HealthGuardian-Setup.exe
```

> ⚠️ `electron/dist/` 已加入 `.gitignore`，安装包**不进 git 仓库**，通过 GitHub Releases 分发。

### 3.3 发布新版本（推荐：打 tag → CI 自动构建）

```bash
# 1. 确保代码已提交
git add .
git commit -m "release: v1.x.x 版本说明"

# 2. 打版本 tag（语义化版本，必须以 v 开头）
git tag v1.0.0
git push origin main --tags

# 3. 等待 GitHub Actions 完成（约 5-8 分钟）
#    查看进度：https://github.com/iamafeng/HealthGuardian/actions
#    完成后 .exe 自动出现在 Releases 页
```

**构建产物固定文件名**：`HealthGuardian-Setup.exe`（见 `electron/package.json` 中 `artifactName`）  
**下载 URL 永久固定**：`https://github.com/iamafeng/HealthGuardian/releases/latest/download/HealthGuardian-Setup.exe`

### 3.4 桌面端使用说明

1.  **连接配置**：启动后若无法连接，界面提供服务端地址输入框，可填入远端地址后保存（永久生效，存于 `%APPDATA%/hg-config.json`）。
2.  **快捷键**：
    - `Ctrl+Shift+H`：全局呼出/隐藏主窗口
    - `Ctrl+Shift+W`：切换健康灵动岛悬浮窗
3.  **系统托盘**：右键托盘图标可快速切换灵动岛、修改服务器地址、账号同步、赏咖啡等。

---

## 3.5 🏝️ 健康灵动岛 (P0 完成)

桌面端悬浮窗，实时展示健康状态，无需打开主窗口即可一览全局。

| 快捷方式 | 说明 |
|---|---|
| `Ctrl+Shift+W` | 切换显示/隐藏灵动岛 |
| 托盘菜单 → `🏝️ 切换健康灵动岛` | 同上 |
| **双击**灵动岛 | 呼出主窗口 |

**显示内容：**
- 🔥 **连续打卡天数** — 实时从后端拉取（每 30 秒刷新）
- ⏱️ **番茄钟倒计时** — 通过 `hg_pomo_state` localStorage 桥接，0 延迟同步
- 💧/🧘 **今日补给 / 调息次数** — 同上后端刷新
- ✅/⚠️ **坐姿状态** — 通过 `hg_posture_bad` localStorage 桥接，探颈时边框变红
- 🌡️ **实时天气** — 来自 `hg_weather` localStorage（由 P5 模块写入）
- 🌙 **免打扰状态** — 进入静默时段时显示
- 📅 **会议倒计时** — 来自 `hg_meeting_end`/`hg_meeting_title` localStorage，显示「X 分后拉伸」

---

## 3.6 🌦️ 环境感知 (P5 完成)

系统自动获取用户所在地实时天气，并联动调整体验：

1. **天气 API**：使用 [Open-Meteo](https://open-meteo.com/) 免费 API，**不需要 API Key**，通过浏览器 Geolocation 自动获取坐标。
2. **授权说明**：首次访问时浏览器会弹出"允许访问位置"请求，允许后生效；拒绝则天气功能静默失效，不影响其他功能。
3. **天气联动行为**：
   - 检测到雨天（含毛毛雨/阵雨/雷暴）→ 自动切换环境音为「🌧️ 雨声」
   - 高温 ≥ 32°C → 提示栏显示「记得多喝水」
   - 低温 ≤ 5°C → 提示「注意保暖补水」
4. **数据流**：天气数据写入 `hg_weather` (localStorage) → 灵动岛 widget 每秒读取同步展示。
5. **刷新频率**：每 30 分钟自动重新获取天气。

---

## 3.7 🧠 心流动态音频 (V4.1 — P3 完成)

CV 驱动的实时专注度感知 + 双耳 Alpha 脑波节律混响，让专注状态可听可感。

### 使用方式

1. 点击专注协议区任意**环境音按钮**（轻柔雨声/森林雨鸣/清晨鸟鸣）启动背景音
2. 点击 **🧠 心流** 按钮激活动态模式（按钮高亮）
3. 启动 **👁️ CV 坐姿感知**（可选，不启动则以满分专注运行）
4. 观察 **心流动态音频指示器** 中的专注状态变化

### 专注度算法

| CV 状态 | 专注得分 | 音频行为 |
|---|---|---|
| 坐姿正常 + 眨眼正常 (4-18次/分) | 1.0 | 音量100%，Alpha 脑波 0.022 |
| 坐姿正常 + 眨眼异常 | 0.6 | 音量85%，Alpha 0.008 |
| 坐姿异常 + 眨眼正常 | 0.55 | 音量85%，Alpha 0.008 |
| 坐姿异常 + 眨眼异常 | 0.15 | 音量50%，Alpha 关闭 |
| 面部不可见 | 0 | 音量50%，Alpha 关闭 |

> **Alpha 脑波实现**：通过 Web Audio API 创建 200 Hz（左耳）和 210 Hz（右耳）正弦波，两耳感知 10 Hz 差频，对应大脑 Alpha 波段（8-13 Hz），有助于放松专注状态。需使用**耳机**以获得双耳节律效果。

---

## 3.8 📅 日程结束提醒 (V4.1 — P5 完成)

会议/专注块结束时自动弹出拉伸引导，防止长时间开会后的久坐伤害。

### 使用方式

1. 点击侧边栏 **📅 日程提醒** 按钮
2. 选择**会议结束时间**（时间格式 HH:MM）
3. 可选填写**日程名称**（如：周会、一对一）
4. 点击 **⏰ 设置提醒**

### 行为说明

| 场景 | 行为 |
|---|---|
| 设置成功 | 侧边栏显示倒计时条 `📅 X 分钟后自动拉伸` |
| 到达结束时间 | 弹出 desk 拉伸序列（颈部→肩部→手腕）|
| 已注册账号 | 同时向 Webhook 发送 `[Health]【日程提醒】...` |
| 时间已过（今天）| 自动顺延至明日同一时间 |
| 刷新页面 | 倒计时自动从 localStorage 恢复（`hg_meeting_end`/`hg_meeting_title`）|
| 点击 ✕ 或「取消已有提醒」| 立即清除提醒并隐藏倒计时条 |

---

## 4. AI 坐姿监测协议 (TensorFlow.js) 🧘

本系统集成了基于 MoveNet 模型的实时姿态识别功能：
1. **启动方式**：进入控制台后，点击"生物传感器"或"开启姿态监测"按钮。
2. **硬件要求**：需连接标准摄像头并授予浏览器/Electron 访问权限。
3. **工作逻辑**：
   - 系统会自动捕捉关键骨骼点（鼻尖、肩膀）。
   - **基准校准**：点击校准按钮记录您的标准坐姿。
   - **实时预警**：一旦检测到"探颈"或"脊柱大幅偏移"超过预设阈值，系统将触发视觉告警并发送 Neural Link 通知。

---

## 5. 钉钉自定义机器人配置教程 🤖

> HealthGuardian 支持通过钉钉自定义机器人将健康提醒、打卡激励、坐姿告警等推送到您的钉钉群，实现多端实时通知。

### 5.1 创建钉钉自定义机器人

**第一步：打开群机器人设置**
1. 打开钉钉，进入您想接收通知的**群聊**（建议创建一个专属的"健康提醒"群）
2. 点击右上角 **群设置（齿轮图标）**
3. 选择 **智能群助手**
4. 点击 **添加机器人**
5. 在机器人列表中选择 **自定义**（Custom）

**第二步：配置机器人基本信息**
- **机器人名字**：建议填写 `HealthGuardian` 或 `健康守护者`
- **头像**：可自定义上传或使用默认

**第三步：安全设置（关键步骤）**

> ⚠️ 安全设置决定了哪些消息可以被机器人接收并推送，**必须正确配置**。

选择 **自定义关键词**，然后填写以下关键词（任填一个即可，建议全填）：

```
Health
提醒
告警
激励
专注
```

> **为什么是这些关键词？**  
> HealthGuardian 发出的所有消息均以 `[Health]` 开头，例如：  
> - `[Health] 【提醒】💧 补给时间到！...`  
> - `[Health] 【激励】afeng 完成了一次补给...`  
> - `[Health] 【机体告警】AI 检测到违规坐姿...`  
> - `[Health] 【搭子督促】你的搭子发现你好久没打卡了！`  
> 
> 只要消息内容包含以上任意一个关键词，钉钉就会通过机器人发出通知。

**第四步：获取 Webhook 地址**
1. 完成安全设置后，点击 **完成**
2. 页面会显示一串 Webhook 地址，格式如下：
   ```
   https://oapi.dingtalk.com/robot/send?access_token=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```
3. 点击 **复制** 保存此地址

### 5.2 在 HealthGuardian 中配置 Webhook

**方法一：通过界面配置（推荐）**
1. 打开 HealthGuardian 控制台
2. 点击侧边栏 **🔔 推送与通知**
3. 将复制的 Webhook 地址粘贴到输入框
4. 勾选 **允许触发远端 Webhook 推送**
5. 点击 **保存配置**

> 💡 Webhook 推送仅对**已注册账号的用户**生效，访客模式不触发远端推送。

**方法二：通过数据库直接设置（开发者）**
```sql
UPDATE t_user 
SET webhook_url = 'https://oapi.dingtalk.com/robot/send?access_token=你的token',
    is_webhook_enabled = 1
WHERE username = '你的用户名';
```

### 5.3 推送消息类型说明

| 触发场景 | 消息示例 |
|---|---|
| 饮水提醒超时 | `[Health] 【提醒】💧 补给时间到！机体需要补充水分...` |
| 久坐提醒超时 | `[Health] 【提醒】🧘 调息时间到！检测到肌肉僵直...` |
| 完成喝水打卡 | `[Health] 【激励】afeng 完成了一次「水分补给」💧` |
| 完成番茄钟 | `[Health] 【专注激励】afeng 完美完成了一个番茄钟（25分钟）🧠` |
| CV 检测坐姿问题 | `[Health] 【机体告警】AI 行为模型侦测到违规坐姿（探颈）...` |
| 连续注视 20 分钟 | `[Health] 【护眼预警】连续注视屏幕已超过 20 分钟...` |
| 搭子电击提醒 | `[Health] 【搭子督促】afeng 发现你好久没打卡了！⚡` |
| 日程结束提醒 | `[Health] 【日程提醒】「周会」已结束，请立即起身活动，执行调息协议！🧘` |

### 5.4 企业微信机器人（可选）

企业微信机器人配置方式类似，Webhook 地址格式为：
```
https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

> HealthGuardian 发送的是通用 JSON 格式（`msgtype: text`），与企业微信机器人格式完全兼容，**无需修改代码**，直接替换 Webhook 地址即可使用。

### 5.5 常见问题排查

| 问题 | 原因 | 解决方案 |
|---|---|---|
| 推送没有收到 | 关键词不匹配 | 检查钉钉机器人安全设置，确保包含 `Health` 或 `提醒` |
| 报错 `access_token is illegal` | Token 已过期或复制不完整 | 在钉钉群重新获取 Webhook 地址 |
| 提示"访客不支持推送" | 当前为匿名模式 | 点击「👤 身份同步」注册账号后再配置 |
| 推送成功但格式乱 | 无 | 正常现象，钉钉 text 类型消息不支持 Markdown |
| 搭子电击没有效果 | 对方未设置 Webhook | 告知搭子也配置好自己的钉钉 Webhook |

---

## 6. 生产环境部署 (Linux) 🚀

```text
服务器端部署目录结构
HealthGuardian/
├── deploy/             # [重要] 待部署包存放处（上传新 JAR 包到此处）
├── bak/                # 历史版本备份目录（脚本自动生成，保留最近3个）
└── sh/                 # 运维运维脚本库
    ├── start.sh        # 智能启动/更新脚本（优先检测 deploy 目录）
    ├── stop.sh         # 安全停止服务脚本
    ├── status.sh       # 服务状态监控
    └── rollback.sh     # 一键回滚至上个备份版本
```

本系统支持基于 `deploy/` 目录的“冷热分离”式部署，确保升级过程中的版本备份真实有效。

1.  **准备阶段**：
    - 在服务器根目录下创建 `deploy` 目录：`mkdir deploy`。
    - 上传 `sh/` 目录到服务器根目录并赋权：`chmod +x sh/*.sh`。

2.  **发布流程**：
    - **上传**：将本地 `target/*.jar` 重命名或直接上传至服务器的 `deploy/` 目录下。
    - **部署/重启**：执行 `sh sh/start.sh`。

3.  **脚本逻辑说明**：
    - 脚本会自动检测 `deploy/` 下是否有新包。
    - 如果有新包：先停止旧进程 -> 将当前运行的包备份到 `bak/` (带时间戳) -> 将新包从 `deploy/` 移动到主目录。
    - 如果没有新包：直接检查并确保当前主目录下的包处于运行状态。

4.  **监控与回滚**：
    - **实时健康度**：`sh sh/status.sh`。
    - **故障回滚**：若新版不稳定，执行 `sh sh/rollback.sh` 快速恢复至上一备份版本。

---

## 7. 更新日志 📋

#### V2.1 增量：清理存量访客数据（升级访客免持久化架构）
> **背景**：V2.1 起，匿名访客不再向 `t_user` 写入记录，其数据仅保存在本地 `localStorage` 与 `t_reminder_config`/`t_reminder_log` 子表中，彻底避免多端同时打开时产生 Key 冲突。若您数据库中已存在旧版自动创建的访客行，执行下方 SQL 一次性清理：

```sql
USE health_guardian;
-- 删除 t_user 中所有访客行（password_hash 为空即代表访客）
-- 其对应的 t_reminder_config 记录保留，不影响正式账号
DELETE FROM t_user WHERE password_hash = '';

-- （可选）同时清理超过 7 天无活跃的孤立访客配置
DELETE FROM t_reminder_config
WHERE secret_key NOT IN (SELECT secret_key FROM t_user)
  AND updated_at < DATE_SUB(NOW(), INTERVAL 7 DAY);
```

#### V2.2 增量：会话感知与本地缓存（无需 SQL，前端 localStorage 键说明）
> **背景**：V2.2 起，`loadData()` 在每次成功从服务器拉取配置后，会同步写入 localStorage 缓存，并根据服务端返回的 `isRegistered` 标志实现三种感知逻辑：

| 场景 | 行为 |
|------|------|
| 有旧 key → 服务端找到配置 → `isRegistered=true`（已绑定账号） | 静默正常加载，显示真实用户名 |
| 有旧 key → 服务端找到配置 → `isRegistered=false`（老访客） | 加载后延迟提示"匿名模式，建议绑定账号" |
| 有旧 key → 服务端未找到配置（key 失效，返回新 key） | 提示"会话已失效，已建立新身份" |
| 服务器不可达（网络异常） | 从 `hg_cached_configs` 恢复配置，进入离线模式 |

> localStorage 新增键：
> - `hg_cached_configs` — 上次成功加载的提醒配置 JSON
> - `hg_cached_username` — 上次成功加载的用户名

---

#### V3.5 增量：健康搭子系统（新增 `t_partner` 表）

> **背景**：V3.5 新增健康搭子功能，需要一张关系表记录好友绑定。Spring Boot 启动时由 `@PostConstruct` **自动建表**，正常情况下无需手动执行。以下脚本供**离线部署 / 手动核查**使用：

```sql
USE health_guardian;

-- 健康搭子关系表（幂等，可重复执行）
CREATE TABLE IF NOT EXISTS t_partner (
    id          BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_key    VARCHAR(255) NOT NULL COMMENT '发起人 secret_key',
    partner_key VARCHAR(255) NOT NULL COMMENT '搭子 secret_key',
    created_at  DATETIME DEFAULT NOW() COMMENT '绑定时间',
    UNIQUE KEY uk_pair (user_key, partner_key)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='健康搭子关系表';
```

> **搭子系统使用说明**：
> - 双方都需已注册账号（`t_user` 中存在记录）。
> - 邀请码 = 自己的**用户名**，将用户名告知搭子输入即可双向绑定。
> - 解绑操作会**同时**删除双向关系记录（`(A→B)` 和 `(B→A)` 都删除）。

---

#### V3.5 增量：新增 API 端点速查表

| 端点 | 方法 | 说明 |
|---|---|---|
| `/api/stats/heatmap` | GET | 返回近 90 天每日打卡计数（热力日历） |
| `/api/stats/weekly-report` | GET | 本周 vs 上周打卡 & 专注时长对比数据 |
| `/api/streak` | GET | 连续打卡天数 & 今日是否已打卡 |
| `/api/daily-brief` | GET | AI 健康日报数据（昨日统计 + 最活跃时段） |
| `/api/partner/my-code` | GET | 获取我的邀请码 & 当前搭子列表 |
| `/api/partner/bind` | POST | 通过用户名绑定搭子 |
| `/api/partner/stats` | GET | 获取所有搭子今日打卡数据 |
| `/api/partner/unbind` | POST | 解除搭子关系（双向删除） |
| `/api/adaptive-schedule` | GET | 分析历史打卡间隔，推荐最优提醒频率 |
| `/api/user/auth` | POST | 统一认证（自动识别新建/登录召回） |

---

#### V3.5 增量：localStorage 新增键说明

| 键名 | 类型 | 说明 |
|---|---|---|
| `hg_theme` | string | 当前主题（`cyber`/`forest`/`ocean`/`light`） |
| `hg_text_style` | string | 提醒文风（`mecha`/`gentle`/`silly`/`strict`） |
| `hg_last_brief` | string | 最后一次展示 AI 日报的日期（防每日重复弹出） |
| `desktop_notify_enabled` | string | 桌面通知开关（`"true"`/`"false"`） |
| `hg_cached_configs` | JSON | 离线缓存的提醒配置 |
| `hg_cached_username` | string | 离线缓存的用户名 |

---

#### V3.5 增量：已知 Bug 修复记录

| Bug | 影响范围 | 修复方式 |
|---|---|---|
| `getWeeklyReport` 查询 `t_pomodoro_log` 时误用 `created_at`（实际字段为 `completed_at`） | 健康周报中「专注时长」数据始终为 0 | 已修正为 `completed_at` |

---

#### V4.1 增量：夜间免打扰（Quiet Hours）

> **SQL 变更**：新增 `t_user` 三列（`quiet_enabled` / `quiet_start` / `quiet_end`）。Spring Boot 启动时 `@PostConstruct` **自动 ALTER TABLE**，无需手动执行。以下供离线部署手动核查：

```sql
USE health_guardian;
ALTER TABLE t_user ADD COLUMN IF NOT EXISTS quiet_enabled TINYINT(1) DEFAULT 1  COMMENT '夜间免打扰开关';
ALTER TABLE t_user ADD COLUMN IF NOT EXISTS quiet_start   VARCHAR(5) DEFAULT '21:00' COMMENT '免打扰开始时间';
ALTER TABLE t_user ADD COLUMN IF NOT EXISTS quiet_end     VARCHAR(5) DEFAULT '07:00' COMMENT '免打扰结束时间';
```

**功能说明**

| 项目 | 说明 |
|---|---|
| 入口 | 🔔 推送与通知 → 开启夜间免打扰 |
| 默认时段 | 21:00 → 07:00（跨午夜，可自定义） |
| 生效范围 | 桌面弹窗通知、Webhook 推送、Toast 提醒 全部静默 |
| 静默策略 | 跳过提醒并重置计时器（避免免打扰结束时连续轰炸）|
| 存储 | 注册用户：同步到 `t_user`；访客：存 `localStorage` |

**新增 API 参数**（扩展原 `POST /api/user/webhook`）

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `quietEnabled` | int | 1 | 免打扰开关（1=开启，0=关闭）|
| `quietStart` | string | `21:00` | 开始时间（HH:mm 格式）|
| `quietEnd` | string | `07:00` | 结束时间（HH:mm 格式）|



> **SQL 变更**：新增 `t_achievement.is_hidden` 字段；新增 4 条隐藏成就数据；新增宠物喂食日志类型 `PET_FEED`。Spring Boot 启动时 `@PostConstruct` **自动完成所有初始化**，正常情况下无需手动执行。以下供离线部署/手动核查：

```sql
USE health_guardian;

-- 1. 给成就表增加 is_hidden 字段（幂等）
ALTER TABLE t_achievement ADD COLUMN IF NOT EXISTS is_hidden TINYINT(1) DEFAULT 0 COMMENT '是否隐藏成就';

-- 2. 写入/更新全套成就数据（含 4 个隐藏成就）
INSERT IGNORE INTO t_achievement (code, name, description) VALUES
('EARLY_BIRD',         '早起鸟',   '08:30 前开启今日首个协议'),
('NIGHT_OWL',          '夜猫子',   '22:00 后依然在执行协议'),
('WATER_BUFFALO',      '大水牛',   '单日生物补给达到 8 次'),
('PERSISTENCE',        '不倒翁',   '连续 7 天坚持监控生命体征'),
('FOCUS_MASTER',       '专注达人', '累计完成 5 次深度专注协议'),
('PRODUCTIVITY_BEAST', '效率怪兽', '累计完成 10 次专注协议'),
('STRETCH_EXPERT',     '拉伸达人', '累计完成 20 次调息指引'),
('COMMUNITY_STAR',     '系统公民', '成功建立加密账号身份'),
('MIDNIGHT_GHOST',     '午夜幽灵', '🔒 隐藏成就：在深夜 00:00-04:00 完成打卡'),
('HYDRO_CHAMPION',     '补水冠军', '🔒 隐藏成就：单日补给达到 10 次'),
('DAWN_WARRIOR',       '黎明战士', '🔒 隐藏成就：07:00 前完成今日首次打卡'),
('PET_LOVER',          '宠物达人', '🔒 隐藏成就：通过小怪兽喂水 5 次');

-- 3. 标记隐藏成就
UPDATE t_achievement SET is_hidden=1 WHERE code IN ('MIDNIGHT_GHOST','HYDRO_CHAMPION','DAWN_WARRIOR','PET_LOVER');
```

**🔧 V4.0 Bug 修复清单**

| Bug | 影响范围 | 修复方式 |
|---|---|---|
| adaptive schedule 查询 `secretKey=?` 应为 `secret_key=?` | 久坐间隔分析始终返回空数据 | 修正 SQL 字段名 |
| `openPartnerModal` 两次串行请求，第一个失败导致第二个丢失 | 搭子功能偶发"加载失败" | 改用 `Promise.all` 并行加载 |
| 侧边栏按钮顺序不合理 | 用户体验差 | 调整为：身份同步→系统设置→推送与通知→健康搭子 |
| forest/ocean 主题无专项 CSS | 弹窗/输入框/按钮在深色主题下样式混乱 | 新增 forest/ocean 各 10+ 条专项覆盖规则 |
| `t_achievement` 无初始数据 | 成就系统显示为空 | `@PostConstruct` 自动幂等插入全套成就数据 |

**✨ V4.0 新功能**

| 功能 | 说明 |
|---|---|
| 🐾 健康小怪兽（P1） | 侧边栏常驻「小健子」宠物，根据今日打卡量从🥚→🤩，坏坐姿变🤒 |
| ⚡ 搭子电击提醒（P2） | 搭子卡片「⚡ 提醒」按钮，通过对方 Webhook 发送督促消息 |
| 🔒 隐藏成就（P4） | 4 个神秘成就（午夜幽灵/补水冠军/黎明战士/宠物达人），解锁时动画弹出 |
| 🖥️ 桌面端同步增强 | Electron 启动后自动注入 `isElectronApp` 标识；托盘菜单新增「账号同步」「赏咖啡」入口；侧边栏底部自动出现跨端同步引导条（已登录显示绿色同步状态） |
| 🌐 页脚 | 全局页脚新增 GitHub 开源链接 + ☕ 赏咖啡按钮（弹出微信/支付宝打赏二维码） |

**🌐 V4.0 新增 API 端点**

| 端点 | 方法 | 说明 |
|---|---|---|
| `/api/partner/nudge` | POST | 发送电击提醒给搭子（通过其 Webhook） |
| `/api/pet/feed` | POST | 宠物喂食记录（累计 5 次解锁 PET_LOVER 成就） |


---

#### V4.3 增量：宠物经济系统 + 精灵灵动岛

> **SQL 变更**：`t_user` 新增 `coins`、`selected_cat`、`unlocked_cats` 三列。`@PostConstruct` 自动执行，无需手动操作。以下供离线部署/手动核查：

```sql
USE health_guardian;
ALTER TABLE t_user ADD COLUMN IF NOT EXISTS coins INT DEFAULT 0 COMMENT '小鱼干币';
ALTER TABLE t_user ADD COLUMN IF NOT EXISTS selected_cat VARCHAR(50) DEFAULT 'cat_OrangeTabby' COMMENT '当前猫咪';
ALTER TABLE t_user ADD COLUMN IF NOT EXISTS unlocked_cats TEXT DEFAULT 'cat_OrangeTabby' COMMENT '已解锁猫咪（逗号分隔）';
```

**✨ V4.3 新功能**

| 功能 | 说明 |
|---|---|
| 🐟 小鱼干币经济系统 | 完成番茄钟自动发放 10 枚小鱼干币（仅注册用户），`coin.wav` 音效反馈 |
| 🛒 猫咪领养商店 | 右键宠物 → 宠物详情 → 商店，消耗小鱼干币解锁 9 种猫咪品种 |
| 🐾 三维健康养成 | 饥饿度/精力值按提醒间隔实时衰减，打卡后立刻回满；清洁度随坏坐姿下降 |
| 🏝️ 宠物精灵接管灵动岛 | `widget.html` 重构为猫咪精灵悬浮窗（140×160），悬停展开数据面板（220×280） |
| 🖱️ 宠物交互重设计 | 左键撸猫（弹跳动画 + 气泡），右键打开详情面板，阻止浏览器默认右键菜单 |

**🌐 V4.3 新增 API 端点**

| 端点 | 方法 | 说明 |
|---|---|---|
| `/api/pet/coins` | GET | 获取小鱼干币数量、当前猫咪、已解锁列表 |
| `/api/pet/earn-coins` | POST | 发放小鱼干币（`amount` 默认 10） |
| `/api/pet/buy-cat` | POST | 购买猫咪（扣除小鱼干币，写入 `unlocked_cats`） |
| `/api/pet/select-cat` | POST | 切换当前猫咪（需已解锁） |

**猫咪价格表**

| 猫咪 | 价格 |
|---|---|
| 橘猫 (OrangeTabby) | 免费（默认） |
| 孟加拉猫 / 三花猫 | 🐟 100 |
| 燕尾服猫 / 黑猫 | 🐟 150 |
| 英短蓝猫 | 🐟 200 |
| 布偶猫 | 🐟 250 |
| 缅因猫 | 🐟 300 |
| 无毛猫 | 🐟 500 |

**🔧 V4.3 Bug 修复清单**

| Bug | 影响范围 | 修复方式 |
|---|---|---|
| `getConfigs` 访客模式下 `userInfos.get(0)` 越界 | `/api/configs` 返回 500 | 将 coins/selectedCat/unlockedCats 的 put 移入 `!userInfos.isEmpty()` 块内 |
| `Pet.interact(e)` 内 `catch(e)` 遮蔽外层参数 `e` | 左键点击宠物无任何反应 | 改为 `catch(_)` |
| `Pet.update()` 末尾缺少 `},` | `Pet` 对象语法错误，整个模块不加载 | 补全闭合符号 |
| 宠物右键同时弹出详情面板和浏览器菜单 | 右键体验混乱 | 添加 `oncontextmenu="event.preventDefault()"` |

**📦 V4.3 前端依赖本地化**

TensorFlow.js 和 Chart.js 改为本地文件，不再依赖 CDN，离线环境可用：

```
static/js/
├── tf-core.min.js          # @tensorflow/tfjs-core@3.21.0
├── tf-converter.min.js     # @tensorflow/tfjs-converter@3.21.0
├── tf-backend-webgl.min.js # @tensorflow/tfjs-backend-webgl@3.21.0
├── pose-detection.min.js   # @tensorflow-models/pose-detection@2.1.0
└── chart.js                # chart.js (latest)
```

**🏝️ 宠物精灵悬浮窗交互说明**

| 操作 | 行为 |
|---|---|
| 左键单击猫咪 | 呼出主窗口 |
| 右键单击猫咪 | 撸猫互动（meow 音效 + play 动画 + 气泡） |
| 鼠标悬停 | 展开数据面板（streak/番茄钟/补给/调息/坐姿/天气/会议） |
| 坐姿异常时 | 猫咪切 sleep 状态，边框变红，气泡提示 |
| `Ctrl+Shift+W` | 切换显示/隐藏宠物精灵窗 |

**localStorage 新增键**

| 键名 | 写入方 | 读取方 | 说明 |
|---|---|---|---|
| `hg_selected_cat` | `Pet.applySelectedCat()` | `widget.html` | 当前猫咪品种，widget 每秒同步精灵图 |
